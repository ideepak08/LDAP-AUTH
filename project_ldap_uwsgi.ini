[uwsgi]
http-timeout = 86400
route-host = ^(?!^localhost$|^127\.0\.0\.1$) rewrite:^(.*)$ http://localhost$1 permanent;
route = .* last:

route-host = ^localhost$ last:
route = .* script_name=project_ldap.wsgi


route = ^/static.* last:
route = ^/media.* last:
socket = 127.0.0.1:8000  # Add this line with your desired host and port
/home/neosoft/Projects/ldap-auth/project_ldap